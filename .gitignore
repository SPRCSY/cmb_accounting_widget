# Gradle
.gradle/
build/

# 本地机器配置（SDK 路径各不相同，不能进仓库）
local.properties

# IDE
.idea/
*.iml
.vscode/

# 系统文件
.DS_Store
Thumbs.db

# 签名文件
# release.jks 是本项目的个人签名，特意入库（本地与 Codespaces 共享同一签名，保证覆盖安装不冲突）
# 其它 keystore/p12 一律排除，防止误提交其它私钥
*.keystore
*.p12
