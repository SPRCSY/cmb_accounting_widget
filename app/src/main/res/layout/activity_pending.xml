<?xml version="1.0" encoding="utf-8"?>
<!-- 待决收入：每笔收入一张卡片，用户选择性质并填抵扣额 -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scrollbars="vertical">

    <LinearLayout
        android:id="@+id/pending_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/pending_intro"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#666666"
            android:lineSpacingExtra="4dp"
            android:text="下面每一笔都是本月捕获到的收入。请判断它的性质，并填写应从本月消费中抵扣的金额（默认填全额，可改为部分 AA）。" />

        <Button
            android:id="@+id/pending_toggle_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="查看已决策历史" />

        <LinearLayout
            android:id="@+id/pending_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="vertical" />

        <Button
            android:id="@+id/pending_back_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="返回" />
    </LinearLayout>
</ScrollView>
